# Compile SCSS to CSS locally

## Requirements

+ SASS gem — either NodeSass, DartSass, or SassLib

## Command

Should look something like this:

```
# Run from the site root:
$ sass --watch assets/scss:assets/css
```
